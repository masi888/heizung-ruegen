export function generateStaticParams() {
  return [] as Array<{ slug: string }>;
}

export default function RatgeberArticlePage() {
  return <main className="p-8">Ratgeber Artikel — Rebuild läuft</main>;
}
